function antes_Depois(){
    let num
    let antes
    let depois

    num = Number(prompt('Informe um número'))
    antes = num-1
    depois = num+1
    alert(`Antes de ${num}, temos ${antes}<br>Depois de ${num}, temos ${depois}`)
}